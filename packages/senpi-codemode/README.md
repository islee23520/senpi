# @code-yeongyu/senpi-codemode

Source-only senpi extension package scaffold for the codemode eval tool.

This package currently exports the no-op extension factory used by the
codemode implementation plan. Runtime tools, kernels, and bridge behavior will
land in later tasks.
