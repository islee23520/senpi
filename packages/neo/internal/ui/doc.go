// Package ui holds the neo TUI's presentational bubbletea components: the
// editor, transcript and tool blocks, markdown renderer, and the overlay
// widgets (model/session/settings pickers and friends). The concrete
// implementation lands in later tasks; this file establishes the package so
// the module builds and tests as a whole.
package ui
