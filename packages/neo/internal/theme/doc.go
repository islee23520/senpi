// Package theme holds the neo TUI's palette and lipgloss styles, transcribed
// from the grok build CLI captures (surfaces, text tiers, accents, borders,
// spinner frames, tool-row glyphs). It also loads custom themes with
// grok-night as the neo default. The concrete implementation lands in a later
// task; this file establishes the package so the module builds and tests as a
// whole.
package theme
