# PR-008 Residual Risks

This work is using code-yeongyu/lazycodex teammode.

- PR-008 intentionally covers command/file approval, permissions approval,
  `request_user_input`, timeout/rejection, `serverRequest/resolved`, and
  secret-answer redaction only.
- PR-009 still owns MCP elicitation and dynamic tool compatibility.
- PR-010 still owns reconnect/resume behavior for pending callbacks after
  disconnect.
- PR-012 still owns a broad redaction QA harness.
- PR-013 still owns the final evidence packet.
